apiVersion: v1
kind: Service
metadata:
  labels:
    app: node-exporter
  name: node-exporter
  namespace: monitoring
spec:
  type: NodePort
  ports:
  - name: metrics
    port: 9100
    targetPort: 9100
    nodePort: 30100
  selector:
    app: node-exporter