apiVersion: v1
kind: ConfigMap
metadata:
  name: loki-values
  namespace: monitoring
data:
  loki-values.yaml: |
    service:
    type: ClusterIP
    ports:
        - name: http
        port: 3100
        targetPort: 3100
    selector:
        app: loki
